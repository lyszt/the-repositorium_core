import { removeLeadingSlash } from "../../740.js";
function getPageKey(route) {
    const cleanRoute = removeLeadingSlash(route);
    return cleanRoute.replace(/\//g, '_').replace(/\.[^.]+$/, '') || 'index';
}
export { getPageKey };
