import { type MarkdownOptions } from '@rspress/shared';
import type { Root } from 'mdast';
import type { VFile } from 'vfile';
export declare const remarkImage: ({ docDirectory, remarkImageOptions, lint, }: {
    docDirectory: string;
    remarkImageOptions?: MarkdownOptions["image"];
    lint?: boolean;
}) => (tree: Root, file: VFile) => void;
