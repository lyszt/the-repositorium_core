import type { ShikiTransformer } from 'shiki';
export declare const SHIKI_TRANSFORMER_ADD_FOLD = "shiki-transformer:add-fold";
export interface ITransformerAddFoldOptions {
    defaultCodeOverflow?: {
        height?: number;
        behavior?: 'fold' | 'scroll';
    };
}
/**
 * @private compile-time only
 */
export declare function transformerAddFold(options?: ITransformerAddFoldOptions): ShikiTransformer;
