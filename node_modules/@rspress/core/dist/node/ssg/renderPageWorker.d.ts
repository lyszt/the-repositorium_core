import type { RouteMeta } from '@rspress/shared';
interface WorkerTask {
    routes: RouteMeta[];
}
declare const exportWorkerGlueFn: ({ routes, }: WorkerTask) => Promise<string[]>;
export default exportWorkerGlueFn;
